<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patient Dashboard - MedAssist</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>Welcome, <%= user.fullName %>!</h1>
        <nav>
            <a href="/profile">Profile</a>
            <a href="/logout">Logout</a>
        </nav>
    </header>

    <section>
        <h2>Your Health Information</h2>

        <!-- Patient Profile Section -->
        <div class="profile-section">
            <img src="<%= user.profilePicture ? '/uploads/' + user.profilePicture : '/images/default-profile.png' %>" alt="Profile Picture">
            <p><strong>Name:</strong> <%= user.fullName %></p>
            <p><strong>Age:</strong> <%= user.age %></p>
            <p><strong>Gender:</strong> <%= user.gender %></p>
            <p><strong>Blood Group:</strong> <%= user.bloodGroup %></p>
            <p><strong>Contact:</strong> <%= user.phone %>, <%= user.email %></p>
        </div>

        <!-- Medical Information -->
        <h3>Medical Details</h3>
        <ul>
            <li><strong>Height:</strong> <%= user.height %> cm</li>
            <li><strong>Weight:</strong> <%= user.weight %> kg</li>
            <li><strong>BMI:</strong> <%= user.bmi %></li>
            <li><strong>Allergies:</strong> <%= user.allergies || 'None' %></li>
            <li><strong>Chronic Diseases:</strong> <%= user.chronicDiseases || 'None' %></li>
            <li><strong>Current Medications:</strong> <%= user.currentMedications || 'None' %></li>
            <li><strong>Past Surgeries:</strong> <%= user.pastSurgeries || 'None' %></li>
            <li><strong>Family Medical History:</strong> <%= user.familyMedicalHistory || 'Not Available' %></li>
            <li><strong>Vaccination Records:</strong> <%= user.vaccinationRecords || 'Not Available' %></li>
        </ul>

        <!-- Emergency Contact & Additional Details -->
        <h3>Emergency & Lifestyle Details</h3>
        <ul>
            <li><strong>Emergency Contact:</strong> <%= user.emergencyContact || 'Not Provided' %></li>
            <li><strong>Insurance Details:</strong> <%= user.insuranceDetails || 'Not Provided' %></li>
            <li><strong>Organ Donor Status:</strong> <%= user.organDonorStatus ? 'Yes' : 'No' %></li>
            <li><strong>Lifestyle Habits:</strong> <%= user.lifestyleHabits || 'Not Provided' %></li>
        </ul>
    </section>

    <footer>
        <p>&copy; 2025 MedAssist. All rights reserved.</p>
    </footer>
</body>
</html>
